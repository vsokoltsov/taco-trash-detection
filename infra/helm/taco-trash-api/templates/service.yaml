apiVersion: v1
kind: Service
metadata:
  name: {{ include "taco-trash-api.fullname" . }}
  labels:
    {{- include "taco-trash-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "taco-trash-api.selectorLabels" . | nindent 4 }}
